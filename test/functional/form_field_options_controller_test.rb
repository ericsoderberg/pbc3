require 'test_helper'

class FormFieldOptionsControllerTest < ActionController::TestCase
  setup do
    @form_field_option = form_field_options(:male)
  end
  
  # nothing really to do here since we always handle options as part of form fields
end
