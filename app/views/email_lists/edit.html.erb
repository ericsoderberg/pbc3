<% title(@email_list.name) -%>

<%= render :partial => 'edit' %>

<h3>Pages</h3>
<ul id="pages">
  <% @pages.each do |page| -%>
    <li><%= link_to page.prefixed_name, friendly_page_path(page) %></li>
  <% end -%>
</ul>
