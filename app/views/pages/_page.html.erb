<%= content_tag_for(:li, page) do %>
  <% if page.feature_image.exists? then %>
  	<%= image_tag page.feature_image.url(:normal) %><br />
  <% end %>
  <%= link_to page.name, friendly_page_path(page) %>
  <%= raw page.feature_phrase %>
<% end %>
