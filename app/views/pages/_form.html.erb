<%= form_for(@page, :html => { :multipart => true }) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<div id="page_content">
  <section>
    <div class="field">
      <%= f.text_area :text, :size => '80x20' %>
    </div>
  </section>

  <aside class="form">
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :image %><br />
      <% if @page.text_image.exists? -%>
        <%= image_tag @page.text_image.url(:thumb) %>
        <%= check_box_tag 'delete_text_image' %> delete image?
      <% end -%>
      <%= f.file_field :text_image %>
    </div>
  </aside>
</div>

  <div class="actions">
    <%= f.submit %> | <%= link_to 'Cancel', friendly_page_path(@page) %>
  </div>
<% end %>
