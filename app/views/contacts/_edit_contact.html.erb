<%= content_tag_for(:li, edit_contact,
  :class => (edit_contact == @contact ? 'active' : '')) do %>
  <%= link_to edit_contact.user.name,
    edit_page_contact_path(@page, edit_contact) %>
<% end %>
