<% if events and not events.empty? -%>

<ul class="events events_list">
  <%= render :partial => 'events/event',
    :collection => events,
    :locals => {:no_name => (no_name || false)} %>
</ul>

<% end -%>
