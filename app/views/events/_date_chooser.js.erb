// event date picker
$(function() {
  $('#date_chooser').change(function() {
    var form = $(this).closest("form");
    form.attr("action", form.attr("action").replace(/events\/\d+\/edit/,
      'events/' + $(this).val() + '/edit'));
    form.submit();
  });
});
