$(function() {
  // shorten list and expand with a 'X more ...' link
  limit = 10;
  if ($('.events_list li')[0]) {      
    var i = 0;
    $(".events_list > li").each(function(){
      i = i+1;
      $(this).toggle(i<=limit);
    });
    if (i>limit) {
      ($('.events_list').append(
        "<li class='more'>" + (i-limit) + " more...</li>").css('cursor', 'pointer').bind(
          "click", function(){
            $(".events_list > li.more").remove();
            var i = 0;
            $(".events_list > li").each(function(){
              i = i+1;
              if (i>limit) {
                $(this).slideDown('slow');
              }
            });
          } )  );
    }
  }
});
