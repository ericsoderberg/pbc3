<%= content_tag_for(:li, authorization) do %>
  <%= authorization.user.name %>
  <div class="delete_gallery_item">
    <%= link_to 'delete', authorization, :confirm => 'Are you sure?',
      :method => :delete, :remote => true %>
  </div>
<% end %>
